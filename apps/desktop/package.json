{
  "name": "@producer/desktop",
  "productName": "Producer Studio",
  "version": "0.2.0",
  "private": true,
  "description": "Producer Studio for desktop — the Producer Studio editor and AI production running locally, synced with your cloud account.",
  "author": "Producer Studio",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "stage": "node scripts/stage.mjs",
    "fetch-tools": "node scripts/fetch-tools.mjs",
    "dist": "node scripts/dist.mjs",
    "typecheck": "tsc --noEmit -p ."
  },
  "devDependencies": {
    "electron": "^44.4.5",
    "electron-builder": "^26.15.3"
  }
}
